# 작업 지시 초안

## 목적

이 프로젝트의 최종 목표는 정규장 시간대에 가상 시장참여자가 거래하는 시뮬레이션을 자동 운영하고, 그 과정의 인프라 지표를 분석하는 과정을 꾸준히 기록하여, 최종적으로 취업용 포르폴리오 프로젝트로 사용하는 것을 목표로 한다.

실제 증권사 주문이나 실제 자산 거래는 범위에 포함하지 않는다. KRX API는 종목·거래일 등 참조 데이터를 적재하는 용도로만 사용한다.

## 현재 구조

- `backend/`: Django/DRF 주문 API와 단일 프로세스 메모리 호가창
- `participant-runner/`: backend에 HTTP 주문을 보내는 별도 시장참여자 프로세스. 추후 다양한 알고리즘을 사용하는 시장참여자로 확장한다.
- 현재 matcher는 메모리 상태를 공유해야 하므로 backend active replica는 1개만 허용한다.
- 트레이더 프로필은 SQLite에 저장되지만 주문·체결·잔고는 아직 영속화하지 않는다.

## 작업 원칙

1. 기능 구현 → 측정 → 병목 관찰 → 다음 기술 도입 순서를 지킨다.
2. KRX 종목 기준정보, 정규장 세션 자동화, 운영 지표·보고서화를 가까운 우선순위로 둔다.
3. Kubernetes, queue, 다중 replica, PostgreSQL 등은 측정으로 필요성이 확인된 뒤에만 도입한다.
4. API 키와 환경별 값은 Git에 넣지 않는다. `.env` 또는 배포 환경의 Secret으로 주입한다.
5. 범위가 커지거나 설계 선택이 필요한 작업은 구현 전에 계획과 영향을 먼저 보고한다.

## 검증과 운영

- 코드 변경 후 관련 테스트와 설정 검증을 실행한다.
- 부하 테스트 결과에는 조건, 처리량, 지연시간, 오류율, 자원 표본과 해석 한계를 함께 남긴다.
- Compose 실행은 요청된 범위에서만 수행하고, 종료 요청 시 컨테이너와 네트워크를 내린다. 데이터 volume 삭제는 명시적으로 요청받았을 때만 한다.
- 커밋·푸시는 사용자가 요청한 경우에만 수행하며, unrelated 변경은 함께 포함하지 않는다.

## 데이터 베이스

- 별도의 PostgreSQL 컨테이너를 활용한다.
- 프로젝트 루트 경로의 db 디렉토리를 활용한다.
- 문서 작성일 KST 2026-07-28 기준으로 하여, KRX의 API 키를 활용해 가장 최신 정규장 종가 데이터를 활용한다. KRX의 API EKY는 해당 디렉토리의 .env 를 참고한다.
- 가져올 데이터는 KOSPI 거래 대금 기준 상위 100 종목이다.
- 주식 종목과 관련 데이터는 추후 확장, 변경될 수 있다.

상세 구현 단계와 완료 기준은 [docs/IMPLEMENTATION_PLAN.md](docs/IMPLEMENTATION_PLAN.md)를 따른다.
